import { rapidapiClient } from '../rapidapiClient.js';import { config } from '../config.js';export async function findEmployeesByTitles(orgName, titles) {  const normalizedOrg = orgName.toLowerCase();  const seen = new Set();  const results = [];  for (const title of titles) {    let users;    try {      const resp = await rapidapiClient.get('/api/v1/search/people', {        params: { name: `${title} ${orgName}`, page: 1 },      });      users = resp.data.data || [];    } catch (err) {      console.error('Search People error for', title, err.response?.status, err.response?.data || err.message);      continue;    }    for (const u of users) {      if (results.length >= config.maxResults) break;      if (!u.company?.toLowerCase().includes(normalizedOrg)) continue;      const url = u.url;      if (seen.has(url)) continue;      seen.add(url);      results.push({        name: u.full_name,        jobTitle: u.title || '(no title)',        linkedinProfileUrl: url,      });    }    if (results.length >= config.maxResults) break;  }  return results;}